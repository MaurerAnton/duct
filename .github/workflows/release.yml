name: release

on:
  push:
    tags: ["v*"]

jobs:
  build:
    strategy:
      matrix:
        include:
          # --- musl static (most portable) ---
          - target: x86_64-linux-musl
            name: duct-x86_64
            static: true
          - target: i386-linux-musl
            name: duct-i386
            static: true
          - target: aarch64-linux-musl
            name: duct-aarch64
            static: true
          - target: arm-linux-musleabihf
            name: duct-armhf
            static: true
          - target: arm-linux-musleabi
            name: duct-arm
            static: true
          - target: riscv64-linux-musl
            name: duct-riscv64
            static: true
          - target: mipsel-linux-musl
            name: duct-mipsel
            static: true
          - target: mips-linux-musl
            name: duct-mips
            static: true
          - target: powerpc64-linux-musl
            name: duct-ppc64
            static: true
          - target: powerpc-linux-musl
            name: duct-ppc
            static: true
          - target: loongarch64-linux-musl
            name: duct-loongarch64
            static: true
          - target: s390x-linux-musl
            name: duct-s390x
            static: true

          # --- glibc dynamic (smallest per-arch) ---
          - target: x86_64-linux-gnu
            name: duct-x86_64-glibc
            static: false
          - target: i386-linux-gnu
            name: duct-i386-glibc
            static: false
          - target: aarch64-linux-gnu
            name: duct-aarch64-glibc
            static: false
          - target: arm-linux-gnueabihf
            name: duct-armhf-glibc
            static: false
          - target: riscv64-linux-gnu
            name: duct-riscv64-glibc
            static: false

          # --- Android Bionic ---
          - target: aarch64-linux-android
            name: duct-aarch64-android
            static: true
          - target: arm-linux-androideabi
            name: duct-arm-android
            static: true
          - target: x86_64-linux-android
            name: duct-x86_64-android
            static: true
          - target: i386-linux-android
            name: duct-i386-android
            static: true

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: 0.13.0
      - run: |
          flags="-target ${{ matrix.target }} -O2 -s"
          ${{ matrix.static }} && flags="$flags -static"
          zig cc $flags -o ${{ matrix.name }} duct.c
          strip ${{ matrix.name }} || true
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: binaries
          merge-multiple: true
      - run: |
          cd binaries
          sha256sum * > SHA256SUMS
      - uses: softprops/action-gh-release@v2
        with:
          files: binaries/*
          generate_release_notes: true
